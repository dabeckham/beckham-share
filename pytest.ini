[pytest]
addopts = -ra -q
# Default run = backend integration tests. Browser E2E (tests/e2e) needs its own
# deps/runtime and is invoked explicitly (scripts/run-e2e.sh, CI e2e job).
testpaths = tests/backend
filterwarnings =
    ignore::DeprecationWarning
